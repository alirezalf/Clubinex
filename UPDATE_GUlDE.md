# راهنمای آپدیت نرم‌افزار برای مشتریان

از آنجایی که شما و همکارتان روی این پروژه کار می‌کنید و از Git استفاده می‌کنید، نیاز دارید تا فایل‌های جدید (آپدیت‌ها) را برای مشتریانی که در حال استفاده از سیستم هستند، به شکل یک «پکیج آپدیت» ارسال کنید.

## فرآیند ساخت فایل آپدیت

ما یک دستور اختصاصی در سیستم قرار داده‌ایم که فایل‌های تغییر یافته را به صورت هوشمند پیدا کرده و در قالب یک فایل `zip` پکیج می‌کند. این فایل `zip` می‌تواند توسط مشتری (یا خود شما) در پنل مدیریت سایت آپلود و اعمال شود.

### مرحله ۱: بیلد گرفتن از فرانت‌اند

اگر تغییراتی در فایل‌های React یا Tailwind و به صورت کلی ظاهر پنل ایجاد کرده‌اید، حتماً قبل از ساخت پکیج، دستور زیر را اجرا کنید تا فایل‌های جدید کامپایل شوند:
```bash
npm run build
```

### مرحله ۲: اجرای دستور ساخت آپدیت

شما باید ابتدا یک commit hash (کد هش مربوط به کامیت قبلی) را پیدا کنید تا سیستم بداند از کدام نسخه تا الآن چه تغییراتی رخ داده است.
می‌توانید با دستور `git log` یا با نگاه به GitHub خود، این کد را پیدا کنید (مثلاً `abcd123`).

سپس از دستور زیر در ترمینال سیستم خودتان استفاده کنید:
```bash
php artisan make:update abcd123
```
* سیستم فایل‌های تغییر یافته از آن زمان تا الان را با دقت استخراج می‌کند.
* فایل‌های پوشه `public/build` که فایل‌های خروجی ظاهر سیستم هستند را نیز به صورت خودکار به پکیج اضافه می‌کند.
* و یک فایل زیپ به نام مثلاً `update-2026-05-31-152010.zip` در روت پروژه برای شما می‌سازد.

## فرآیند اعمال آپدیت در مشتری (آنلاین/آفلاین)

وقتی پکیج `zip` تولید شد، آن را برای مشتری ارسال کنید (یا روی سیستم آفلاین مشتری ببرید).

1. ابتدا حتماً از دیتابیس فعلی مشتری یک **بک‌آپ** دریافت کنید. در پنل ادمین بخش تنظیمات > ابزارهای سیستم امکان آن فراهم است.
2. در پنل ادمین، وارد بخش **تنظیمات سیستم > ابزارهای سیستم** شوید.
3. در قسمت جدید با عنوان «آپلود و نصب بسته بروزرسانی»، فایل سیستم آپدیت که ساختید را انتخاب و دکمه را بزنید.

سیستم به صورت خودکار:
- فایل‌های جدید کدها را جایگزین فایل‌های سیستم می‌کند.
- جداول جدید دیتابیس (Migration) را به صورت خودکار اجرا و دیتابیس را ارتقا می‌دهد. (بدون پاک شدن اطلاعات مشتری)
- کش سیستم، مسیرها و کش تنظیمات را پاکسازی و بهینه می‌کند.

**نکته امنیتی و فنی بسیار مهم:** 
اگر در فرآیند توسعه، پکیج جدیدی از طریق `composer require` نصب کرده‌اید، توجه داشته باشید که فایل‌های داخل پوشه `vendor` باید با این بروزرسانی به هاست مشتری منتقل شوند. از آنجا که `vendor` در سیستم فایل تغییرات (git) نیست، در فایل `.zip` قرار نمی‌گیرد و دستورات `composer` روی هاست اشتراکی قابل اجرا نیستند. بنابراین در این شرایط خاص، باید پوشه `vendor` جدید خودتان را دستی برای مشتری ارسال کنید تا جایگزین پوشه قدیمی کند.

بدین ترتیب مشتری همیشه آپدیت‌ها را به سادگی و بدون خطرات از هم پاشیدگی دیتایش دریافت می‌کند.
