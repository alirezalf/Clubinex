# چک‌لیست بررسی پروژه Clubinex

تاریخ تهیه: ۲۳ سپتامبر ۲۰۲۶

## معنی وضعیت‌ها

- `[تأیید شده]` یعنی برای آن بخش بررسی اجرا شده و خطای مشخصی دیده نشده است.
- `[بررسی ایستا]` یعنی مسیر، نام route، نوع‌ها یا syntax بررسی شده؛ اما سناریوی کامل کاربر در مرورگر و دیتابیس واقعی اجرا نشده است.
- `[نیازمند تست عملی]` یعنی هنوز نمی‌توان سلامت عملکرد آن بخش را اعلام کرد.
- `[اصلاح شده]` یعنی باگ قطعی پیدا و در فایل مربوط رفع شده است.

> این چک‌لیست به معنی تأیید کامل همهٔ قابلیت‌ها نیست. پروژه ده‌ها جریان عملیاتی دارد و برای تأیید کامل، هر موردِ «نیازمند تست عملی» باید با دیتابیس و تنظیمات واقعی اجرا شود.

## بررسی‌های عمومی

- [تأیید شده] Syntax تمام ۲۵۷ فایل PHP بررسی شد.
- [تأیید شده] TypeScript بدون خطا اجرا شد.
- [تأیید شده] Build فرانت‌اند با Vite اجرا و manifest تولید شد.
- [تأیید شده] همهٔ نام‌های route ثابت که در فایل‌های TSX استفاده شده‌اند، در Laravel وجود دارند.
- [تأیید شده] cache کردن routeهای Laravel بدون خطا اجرا شد.
- [تأیید شده] تست‌های موجود پروژه اجرا شدند: ۱۸ تست و ۳۵ assertion بدون failure.
- [نیازمند تست عملی] تست‌های موجود همهٔ فرم‌ها و همهٔ کنترلرها را پوشش نمی‌دهند.

## صفحات عمومی و نصب

- [بررسی ایستا] صفحهٔ اصلی و اسلایدر (`Welcome`).
- [بررسی ایستا] صفحهٔ دربارهٔ ما.
- [بررسی ایستا] robots.txt و sitemap.
- [بررسی ایستا] صفحه و فرایند نصب اولیه (`Setup`).
- [نیازمند تست عملی] نصب روی دیتابیس خالی، اجرای migration و seed.
- [نیازمند تست عملی] کپچا در محیطی که GD و تنظیمات کپچا فعال هستند.

## احراز هویت و امنیت حساب

- [تأیید شده] نمایش صفحهٔ ورود، ورود با رمز، خطای ورود نامعتبر و خروج؛ در تست‌های پروژه پوشش دارند.
- [تأیید شده] نمایش ثبت‌نام، ثبت‌نام کاربر و اعتبارسنجی شماره موبایل؛ در تست‌های پروژه پوشش دارند.
- [بررسی ایستا] ورود و تأیید OTP، محدودیت درخواست OTP و lock screen.
- [بررسی ایستا] بازیابی رمز، تأیید ایمیل و احراز هویت دومرحله‌ای Fortify.
- [نیازمند تست عملی] ارسال واقعی SMS/OTP، ایمیل بازیابی، کپچا و 2FA در محیط واقعی.

## داشبورد و تنظیمات کاربر

- [تأیید شده] دسترسی کاربر واردشده به داشبورد و جلوگیری از ورود مهمان؛ در تست‌ها پوشش دارند.
- [بررسی ایستا] آمار داشبورد، میانبرها و ذخیره‌سازی Quick Access.
- [نیازمند تست عملی] صحت اعداد داشبورد با دادهٔ واقعی، کش و نقش‌های مختلف کاربری.

## پروفایل، شهر و نمایندگی

- [تأیید شده] نمایش و ذخیرهٔ اطلاعات پایهٔ پروفایل در تست‌های پروژه پوشش دارند.
- [تأیید شده] تغییر ایمیل و ریست/حفظ وضعیت تأیید ایمیل در تست‌ها پوشش دارند.
- [اصلاح شده] ذخیرهٔ شهر فقط برای استان انتخاب‌شده معتبر است.
- [اصلاح شده] ایجاد نمایندگی، اتصال `agent_id` به کاربر و حذف نمایندگی هنگام خاموش‌کردن گزینهٔ نمایندگی اصلاح شد.
- [بررسی ایستا] آپلود تصویر پروفایل و مدیریت نشست‌ها.
- [نیازمند تست عملی] آپلود فایل واقعی، حذف فایل قدیمی، خروج از نشست‌های دیگر و تأیید نمایندگی توسط ادمین.

## محصولات و سریال

- [بررسی ایستا] نمایش محصولات، ثبت سریع با سریال، ثبت کامل با فاکتور، ویرایش و حذف درخواست ثبت محصول.
- [اصلاح شده] تأیید ثبت دستی محصول نمی‌تواند سریالِ مصرف‌شده توسط کاربر دیگر را به نام متقاضی جدید ثبت کند.
- [بررسی ایستا] ثبت سریال‌ها، تولید سریال، import سریال و بررسی سریال از سمت ادمین.
- [بررسی ایستا] همگام‌سازی محصول و دسته‌بندی با WordPress.
- [نیازمند تست عملی] آپلود تصویر/فاکتور، تأیید یا رد درخواست، ثبت امتیاز، rollback امتیاز، import فایل Excel و sync واقعی WordPress.

## امتیاز، پاداش و باشگاه

- [بررسی ایستا] قوانین امتیاز، تراکنش‌های امتیاز، سطح باشگاه و عضویت/ارتقا.
- [اصلاح شده — بررسی ایستا] فیلد هزینهٔ نقدی جایزه در مدل، اعتبارسنجی و فرم مدیریت تکمیل شد؛ پیش از این لایهٔ سرویس آن را می‌خواند، اما در مدل/فرم قابل ذخیره نبود.
- [اصلاح شده — بررسی ایستا] اعتبار دریافت جایزه اکنون تاریخ انقضا، موجودی کیف پول و درخواست‌های ردشده را درست در نظر می‌گیرد.
- [اصلاح شده — بررسی ایستا] تغییر وضعیت درخواست جایزه با قفل رکورد و گذارهای مجاز انجام می‌شود تا رد/تبدیل/بازگشت وجه و امتیاز در درخواست موازی یا ارسال تکراری دوباره اعمال نشود.
- [اصلاح شده — بررسی ایستا] کسر و بازگشت وجه نقدی جایزه با قفل رکورد کیف پول انجام می‌شود.
- [بررسی ایستا] تاریخچهٔ پاداش کاربران در پنل ادمین.
- [نیازمند تست عملی] هم‌زمانی تراکنش‌های امتیاز، کمبود امتیاز، لغو پاداش، ارتقای سطح و محاسبهٔ همهٔ قوانین امتیاز.

## کیف پول و پرداخت

- [اصلاح شده] callback پرداخت فقط برای مالک تراکنش قابل تأیید است.
- [اصلاح شده] callback تکراری، موجودی کیف پول را دوباره افزایش نمی‌دهد.
- [تأیید شده] تست عدم دسترسی کاربر دیگر به callback تراکنش کیف پول اضافه و اجرا شد.
- [اصلاح شده] رد درخواست برداشت دیگر به‌دلیل `clone` شدن مقدار عددی خطای PHP نمی‌دهد و مبلغ بازگردانده می‌شود.
- [اصلاح شده] برداشت به تراکنش کیف‌پول خودش متصل شد تا پرداخت برداشت هم‌مبلغ، تراکنش اشتباه را تأیید نکند.
- [اصلاح شده — بررسی ایستا] تبدیل امتیاز/کیف پول و درخواست برداشت، موجودی را پس از قفل‌کردن رکوردهای کاربر و کیف پول دوباره کنترل می‌کنند؛ بنابراین بررسیِ کهنهٔ موجودی در درخواست هم‌زمان مبنای کسر قرار نمی‌گیرد.
- [اصلاح شده — بررسی ایستا] شماره کارت برداشت باید دقیقاً ۱۶ رقم باشد، نه صرفاً ۱۶ نویسه.
- [نیازمند تست عملی] اتصال واقعی درگاه پرداخت، callback با authority واقعی، رد/تأیید/پرداخت برداشت و آزمون هم‌زمانی مالی با دیتابیس واقعی.

## نظرسنجی و گردونه شانس

- [اصلاح شده — بررسی ایستا] ارسال پاسخ نظرسنجی اکنون فقط سؤال‌های همان نظرسنجی را می‌پذیرد، سؤال تکراری و سؤال اجباریِ جاافتاده را در سرور رد می‌کند و گزینهٔ چندگزینه‌ای را نیز اعتبارسنجی می‌کند.
- [اصلاح شده — بررسی ایستا] محدودیت زمانی نظرسنجی علاوه بر تایمر ظاهری، در نشست سمت سرور کنترل می‌شود.
- [اصلاح شده — بررسی ایستا] امتیاز آزمون فقط از مسیر تجمیعی کنترلر ثبت می‌شود؛ مسیر دومِ ثبت امتیاز هر پاسخ حذف شد تا امتیاز تکراری ایجاد نشود.
- [اصلاح شده — بررسی ایستا] شمارش نوبت شرکت، پاسخ‌های متعدد یک کاربر را چند نوبت محسوب نمی‌کند. ساختار دادهٔ فعلی عملاً فقط یک نوبت پاسخ را پشتیبانی می‌کند و پنل ادمین نیز همین مقدار را ایجاد می‌کند.
- [اصلاح شده — بررسی ایستا] نوع سؤال `rating` که در پنل ادمین قابل ایجاد بود ولی در فرم کاربر نمایش نداشت، به فرم اضافه شد.
- [بررسی ایستا] مدیریت نظرسنجی‌ها و سؤال‌ها در ادمین.
- [اصلاح شده — بررسی ایستا] گردونه در صورت پر بودن سقف روزانه، موجودی جایزه را کم نمی‌کند و چرخش رایگانِ retry با مبلغ پرداختی صفر ثبت می‌شود.
- [بررسی ایستا] چرخاندن گردونه، جایزه‌ها و مدیریت جایزه‌ها.
- [نیازمند تست عملی] جلوگیری از پاسخ یا چرخش تکراری، محدودیت زمانی، ثبت امتیاز و احتمال/موجودی جایزه.

## تیکت و پشتیبانی

- [اصلاح شده — بررسی ایستا] فهرست تیکت‌ها دیگر همهٔ پاسخ‌های ادمین را صرفاً با باز شدن فهرست «خوانده‌شده» نمی‌کند؛ تغییر وضعیت فقط هنگام باز کردن همان تیکت انجام می‌شود.
- [بررسی ایستا] فهرست و پاسخ تیکت در پنل ادمین.
- [نیازمند تست عملی] تخصیص خودکار اپراتور، وضعیت‌های گفتگو، هم‌زمانی پاسخ‌ها و rate limit ارسال تیکت.

## اعلان‌ها، ارجاع و کمک

- [بررسی ایستا] فهرست اعلان، خواندن تکی/همه و حذف اعلان.
- [بررسی ایستا] ارسال اعلان گروهی از پنل ادمین و قالب‌های اعلان.
- [بررسی ایستا] صفحهٔ ارجاع، لینک دعوت و گزارش ارجاع.
- [بررسی ایستا] مرکز راهنما و محتوای آن.
- [نیازمند تست عملی] صف ارسال اعلان، ایمیل/SMS واقعی، محاسبهٔ کمیسیون ارجاع و نمایش درست داده‌های help.

## مدیریت ادمین

- [بررسی ایستا] کاربران، نقش‌ها، محصولات، دسته‌بندی‌ها، پاداش‌ها، باشگاه و تنظیمات.
- [بررسی ایستا] اسلایدرها، اسلایدها، گزارش‌های ثابت و گزارش‌ساز پویا.
- [بررسی ایستا] قالب‌های ایمیل، قالب‌های SMS، تنظیمات ایمیل/SMS و تست اتصال‌ها.
- [بررسی ایستا] لاگ سیستم، پشتیبان‌گیری و بستهٔ به‌روزرسانی.
- [نیازمند تست عملی] مجوزهای هر نقش، عملیات حذف، آپلود update، بکاپ واقعی، اجرای صف، import/export و همهٔ اتصال‌های بیرونی.

## API و اتصال‌های بیرونی

- [بررسی ایستا] API شهرها، جستجوی کاربران، global search و endpointهای داخلی.
- [بررسی ایستا] API امتیاز کاربر و webhook پرداخت تعریف شده‌اند.
- [نیازمند تست عملی] احراز هویت webhook، API امتیاز در محیط واقعی، WordPress، SMS، Email و payment gateway.

## موارد لازم پیش از انتشار

- [ ] اجرای `php artisan migrate --force` روی نسخهٔ پشتیبان دیتابیس.
- [ ] اجرای `npm run build` در محیط deployment.
- [ ] فعال‌بودن PHP GD برای کپچا و Excel/Spreadsheet.
- [ ] استفاده از PHP سازگار با قفل وابستگی‌ها؛ نسخهٔ فعلی lock برای `phpoffice/phpspreadsheet` با PHP 8.5 سازگار نیست.
- [ ] تنظیم `.env` واقعی برای database، queue، cache، mail، SMS، WordPress و payment gateway.
- [ ] اجرای تست عملی همهٔ موارد «نیازمند تست عملی» با نقش کاربر، ادمین و نماینده.

## بازبینی ایستای تکمیلیِ موارد گزارش‌شده — ۲۳ سپتامبر ۲۰۲۶

### باشگاه مشتریان

- [اصلاح شده — بررسی ایستا] پروفایل: فرم آواتار با `FormData` ارسال می‌شود و route ارسالی به `profile.update` اصلاح شد. کنترلر نیز فایل را ذخیره و URL را ثبت می‌کند. نمایش واقعی فایل همچنان به مجوز نوشتن و لینک `public/storage` سرور وابسته است.
- [اصلاح شده — بررسی ایستا] کیف پول: شکست درخواست درگاه، تراکنش را از حالت `pending` خارج می‌کند؛ تبدیل اعتبار کیف پول به امتیاز نیز با قفل کیف پول/کاربر بازبینی و اصلاح شده است.
- [بررسی ایستا؛ مشکلی نداشت] ثبت محصول: دو دکمهٔ «سریال» و «فاکتور» به دو mode متفاوت (`simple` و `advanced`) می‌روند؛ ادغام آنها لازم نیست.
- [بررسی ایستا؛ مشکلی نداشت] تصویر محصول و فاکتور: فرانت مسیرهای نسبی `/storage` و `/uploads` را می‌پذیرد و کنترلرها URL را ارسال می‌کنند. اگر تصویر در اجرا دیده نمی‌شود، علت خارج از سورس است: فایل واقعی، دسترسی پوشه یا `php artisan storage:link`.
- [اصلاح شده — بررسی ایستا] تأیید درخواست محصول: وضعیت تأیید به‌صورت صریح در payload ارسال می‌شود و ذخیرهٔ سریال دیگر پس از اعطای امتیاز بی‌صدا خطا را پنهان نمی‌کند.
- [بررسی ایستا؛ مشکلی نداشت] جوایز من: لغو درخواست در وضعیت `pending` وجود دارد و امتیاز/اعتبار را بازمی‌گرداند.
- [بررسی ایستا؛ مشکلی نداشت] گردونه: در تاریخچهٔ جایزهٔ صفر، «—» نمایش داده می‌شود؛ عبارت «رایگان» وجود ندارد.
- [اصلاح شده — بررسی ایستا] گردونه: اندازهٔ پیش‌فرض فونت بخش‌های چرخ کاهش یافت.
- [اصلاح شده — بررسی ایستا] تیکت: بازکردن همان تیکت پاسخ‌داده‌شده، badge را حذف می‌کند؛ فهرست تیکت‌ها دیگر همهٔ پاسخ‌ها را یک‌جا تغییر وضعیت نمی‌دهد.

### پنل مدیریت

- [اصلاح شده — بررسی ایستا] عملیات گروهی کاربران: شناسه‌های انتخابی و نوع عملیات مودال با propهای فعلی همگام می‌مانند.
- [بررسی ایستا؛ مشکلی نداشت] دسترسی‌های کاربر/نقش: فرم ویرایش آرایهٔ permission را ارسال و کنترلر `syncPermissions` را اجرا می‌کند.
- [بررسی ایستا؛ مشکلی نداشت] همگام‌سازی وردپرس: مودال دسته‌ها و محصولات صفحات API را تا پاسخ `finished` ادامه می‌دهد. ناقص بودن دادهٔ اجراشده می‌تواند از پاسخ WordPress، اعتبار کلیدها یا قطع عملیات باشد.
- [اصلاح شده — بررسی ایستا] شمارندهٔ درخواست‌های محصول در آیتم والد منوی مدیریت محصول نیز نمایش داده می‌شود.
- [بررسی ایستا؛ مشکلی نداشت] ویرایش موجودی محصول در صورت نداشتن فایل جدید، تصویر قبلی را حفظ می‌کند.
- [اصلاح شده — بررسی ایستا] گزارشات: جستجوی زندهٔ ۶۰۰ میلی‌ثانیه‌ای برقرار است؛ کوئری‌های گزارش محصول/سریال/درخواست گروه‌بندی شدند تا `OR` فیلترهای دیگر را دور نزند و نام کوچک کاربر نیز جستجو شود.
- [بررسی ایستا؛ مشکلی نداشت] مدیریت جوایز: جستجوی نام/موبایل کاربر، نام جایزه و کد رهگیری در بک‌اند وجود دارد؛ حذف جایزه نیز در فهرست ادمین پیاده‌سازی شده است.
- [اصلاح شده — بررسی ایستا] بازگشت امتیاز جایزه: تغییر وضعیت درخواست در سرویس با قفل و گذار وضعیت معتبر انجام می‌شود؛ مسیر `converted` به `rejected` برای برگشت امتیاز نیز مجاز شد.
- [بررسی ایستا؛ مشکلی نداشت] مدیریت اسلایدر و تنظیمات باشگاه: فرم‌ها فایل را با `forceFormData` ارسال و کنترلرهای مربوط فایل را ذخیره می‌کنند.
- [اصلاح شده — بررسی ایستا] تنظیمات سیستم: فایل‌های لوگو، فاوآیکون، تصویر پس‌زمینه و لوگوی ورود اکنون همراه ورودی‌های متنی وارد پردازش کنترلر می‌شوند و با نام امن ذخیره می‌گردند.
- [بررسی ایستا؛ مشکلی نداشت] فعال/غیرفعال‌سازی کپچا و ruleهای فرم ورود/ثبت‌نام به تنظیم امنیتی متصل‌اند. تولید تصویر کپچا وابسته به نصب `mews/captcha` و افزونهٔ PHP GD روی سرور است.
- [بررسی ایستا؛ مشکلی نداشت] بکاپ: کد پیش از اجرای `mysqldump` غیرفعال‌بودن `exec` را تشخیص داده و پیام راهنما می‌دهد. ایجاد فایل بکاپ MySQL همچنان به مجازبودن `exec` و در دسترس‌بودن `mysqldump` در سرور وابسته است.
- [اصلاح شده — بررسی ایستا] پاسخ تیکت مدیر: پیوست اعتبارسنجی‌شده اکنون در `ticket-attachments` ذخیره و URL آن همراه پیام ثبت می‌شود.

### وابستگی‌های بیرونی که صرفاً با بازبینی کد تأیید نمی‌شوند

- [نیازمند تنظیم سرور] مرچنت‌کد، sandbox و دسترسی شبکهٔ درگاه زرین‌پال.
- [نیازمند تنظیم سرور] لینک storage، دسترسی نوشتن پوشه‌های `storage` و `public/uploads`.
- [نیازمند تنظیم سرور] PHP GD و پکیج/پیکربندی کپچا.
- [نیازمند تنظیم سرور] queue worker یا قابلیت اجرای `afterResponse` برای ارسال واقعی SMS و ایمیل؛ کد dispatch و template را دارد، ولی صحت ارسال بیرونی بدون اجرا قابل تأیید نیست.

## تکمیل بازبینی فرم‌ها و پیام‌ها — ۲۳ سپتامبر ۲۰۲۶

- [اصلاح شده — بررسی ایستا] آواتار پروفایل: انتخاب فایل غیرتصویری یا بزرگ‌تر از ۵ مگابایت، پیام فوری و قابل مشاهده دارد؛ خطای اعتبارسنجی بک‌اند نیز کنار آواتار نمایش داده می‌شود.
- [اصلاح شده — بررسی ایستا] کامپوننت عمومی `FormFile`: محدودیت اندازه را به پیام درون فرم تبدیل می‌کند تا خطا صرفاً در alert گم نشود؛ فرم محصول حد ۲ مگابایتِ بک‌اند را به همان کامپوننت منتقل می‌کند.
- [اصلاح شده — بررسی ایستا] فرم ثبت فاکتور اکنون PDF را نیز در انتخاب فایل اجازه می‌دهد، همان‌طور که `RegisterProductRequest` در سرور اجازه می‌دهد.
- [اصلاح شده — بررسی ایستا] خطای اعتبارسنجی تصویر در ویرایش باشگاه و ایجاد/ویرایش جایزه کنار فیلد فایل نمایش داده می‌شود.
- [اصلاح شده — بررسی ایستا] پیام‌های فرمت و حجم فایل برای آواتار، محصول، فاکتور و جایزه صریح و فارسی شدند.
- [اصلاح شده — بررسی ایستا] Toast سراسری اکنون هر سه کلید `message`، `success` و `error` را نمایش می‌دهد؛ بنابراین پاسخ موفقی که کنترلر با `with('success', ...)` برمی‌گرداند، بی‌صدا نمی‌ماند.
